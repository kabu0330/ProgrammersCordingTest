#pragma once

#include <string>
#include <vector>
#include <stack>

using namespace std;

int solution2(vector<vector<int>> board, vector<int> moves) {
    
    stack<int> lanes[31];
    
    for (size_t i = board.size() - 1; i >= 0; i++)
    {

    }

    int answer = 0;

    return answer;
}